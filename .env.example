# PHP API and Python crawler
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=wesiper
DB_USER=wesiper
DB_PASSWORD=change-me

# PHP only: public-data portal decoded or URL-encoded service key.
KMA_SERVICE_KEY=
# Optional private PHP config (outside the web root), returning ['KMA_SERVICE_KEY' => '...'].
WESIPER_WEATHER_CONFIG=
WESIPER_WEATHER_CACHE_DIR=

# Vite development server only
VITE_API_PROXY_TARGET=http://localhost:8080
